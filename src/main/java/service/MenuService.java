package service;

import java.sql.SQLException;

import javax.management.Query;

import connexion.HttpConnexion;

public class MenuService {

	public void post() {
		/*
		 * conn.connect(); try { String
		 * query="insert into tb_pharma(nom,prix,quantite,mt_total,payer,reste) values(?,?,?,?,?,?)"
		 * ;
		 * 
		 * conn.setPst(conn.getConnection().prepareStatement(query)); } catch
		 * (SQLException e) {
		 * 
		 * e.printStackTrace(); }
		 */
		
	}

	
}
